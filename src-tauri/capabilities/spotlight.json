{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "spotlight",
  "description": "Capability for the spotlight quick-access window. Window-management and hotkey permissions are added in later phases as those calls are introduced.",
  "windows": ["spotlight"],
  "permissions": ["core:default"]
}
