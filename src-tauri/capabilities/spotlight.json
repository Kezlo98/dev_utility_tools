{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "spotlight",
  "description": "Capability for the spotlight quick-access window. core:default is intentionally sufficient: all window show/hide/resize and hotkey rebind operations are performed through custom Rust commands in spotlight_cmd.rs, so this WebView only invokes commands and listens for events.",
  "windows": ["spotlight"],
  "permissions": ["core:default"]
}
