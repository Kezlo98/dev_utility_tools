{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main DevKit window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
